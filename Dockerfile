# Использую официальный базовый образ
FROM node:18-alpine

# Создаю рабочую директорию
WORKDIR /app

# Копирую файлы проекта
COPY . .

# Устанавливаю зависимости (если есть)
RUN npm install

# Указываю команду запуска
CMD ["node", "app.js"]
